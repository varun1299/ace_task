# ACE Guardian API base URL (used by C# and Python tests)
# Copy to .env and adjust if your API runs elsewhere.
GUARDIAN_API_BASE_URL=http://localhost:5000

# Request timeout in seconds for API calls (Python tests)
TIMEOUT=10
